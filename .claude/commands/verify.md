---
description: Run the full verification loop using the verify-app agent
---

Changed files in this session:
```
$(git diff --name-only HEAD)
```

Invoke the `verify-app` subagent to run the full verification sequence on these changes. Do not summarize — show the full verification report.
