# Project Rules

## Build & Verify
<!-- Replace these with your actual commands -->
- Build: `<your build command>`
- Typecheck: `<your typecheck command>`
- Test: `<your test command>`
- Lint: `<your lint command>`
- Format: `<your format command>`

Run all of these before opening a PR. Never suppress warnings to make a build pass.

## Workflow
- Start non-trivial tasks in Plan mode (Tab to switch). Refine the plan before executing.
- Use `@code-architect` before implementing anything that touches 3+ files.
- Use `@code-simplifier` after every implementation.
- Use `@verify-app` before every PR.

## Rules
<!-- This section grows over time. When the model makes a mistake, add a rule here and commit it. -->

## Off-Limits Without Explicit Instruction
<!-- Add protected paths or files specific to your project here -->
